# It tells Python that ingestion is a package. That allows us to write imports like:

# from src.ingestion.reader import DataReader

# Without it, imports can become unreliable depending on how the project is run